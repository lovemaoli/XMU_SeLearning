3.程序运行时一个变量占有内存空间的时间段称为生存期。
生存期分为静态生存期、自动生存期、动态生存期。

4.限制标识符的可见性、保证作用域不相同的两个标识符可以相同。
作用域分为：局部作用域、全局作用域、文件作用域、函数作用域、函数原型作用域、类作用域

6.宏可以解决对小函数频繁调用效率不高的问题，但是有时会出现重复计算、不进行参数类型的检查和调换、不利于一些工具对程序的处理

14.从上到下分为1，2，3
第一个调用了函数1；
第二个调用了函数2；
第三个调用了函数2；
第四个调用了函数3；
第五个产生歧义，可能调用函数1或2；

15.两个f分别表示函数名和变量名称，不存在重载，使用时区分是否带（）即可。

16.防止同一个内联函数的各个定义之间的不一致