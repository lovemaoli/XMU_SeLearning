1.抽象：包含过程抽象和数据抽象，过程抽象是将一些功能抽象为子函数，数据抽象是针对某个数据描述出能对其实施的所有操作以及这些操作之间的关系；
封装：包含过程封装和数据封装，是指把一个程序实体的具体实现细节对使用者进行隐藏的机制。

2.将类的定义放在头文件中，将成员函数的实现代码放在一个源文件中

3.定义一个类时可以规定对于外部成员的访问权限，分为public、private、protected

